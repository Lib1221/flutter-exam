# Setup

```bash
flutter pub get
flutterfire configure
flutter run
```

Enable Authentication and Cloud Firestore in the Firebase console. Seed a few `questions` documents to see content.

Checks: `flutter analyze`, `flutter test`.
